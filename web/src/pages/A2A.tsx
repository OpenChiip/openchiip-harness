// Copyright: (c) OpenChiip Organization. https://github.com/OpenChiip/openchiip-harness
// Copyright: (c) <opensource@openchiip.com>
// Generated By : OpenChiip
// Released under the AIGCGPL-1.0 License. For more information, see [https://github.com/OpenChiip/AIGCGPL/blob/main/LICENSE].
// This code was generated using a generative artificial intelligence system and is licensed under the Artificial Intelligence Generated Code General Public License (AIGCGPL).

import { useQuery } from '@tanstack/react-query'
import { api } from '@/lib/api'
import { Users, User, Globe } from 'lucide-react'

export function A2A() {
  const { data: card } = useQuery({ queryKey: ['agentCard'], queryFn: api.getAgentCard })
  const { data: peers } = useQuery({ queryKey: ['peers'], queryFn: api.getPeers })

  return (
    <div className="p-6 space-y-6">
      <div>
        <h2 className="text-2xl font-bold">Agent 协作</h2>
        <p className="text-muted-foreground">Agent-to-Agent 名片与发现</p>
      </div>

      {/* Agent Card */}
      {card && (
        <div className="rounded-xl border bg-card p-6">
          <div className="flex items-center gap-3 mb-4">
            <div className="flex h-12 w-12 items-center justify-center rounded-xl bg-primary/10">
              <User className="h-6 w-6 text-primary" />
            </div>
            <div>
              <h3 className="font-semibold text-lg">{card.name}</h3>
              <p className="text-sm text-muted-foreground">{card.agent_id}</p>
            </div>
          </div>
          <div className="grid gap-3 sm:grid-cols-2">
            <div className="rounded-lg bg-muted p-3">
              <p className="text-xs text-muted-foreground">地址</p>
              <p className="font-mono text-sm">{card.host}:{card.port}</p>
            </div>
            <div className="rounded-lg bg-muted p-3">
              <p className="text-xs text-muted-foreground">Chat URL</p>
              <p className="font-mono text-sm truncate">{card.chat_url}</p>
            </div>
          </div>
          {card.skills.length > 0 && (
            <div className="mt-4">
              <p className="text-sm font-medium mb-2">技能</p>
              <div className="flex gap-1 flex-wrap">
                {card.skills.map((s) => (
                  <span key={s.name} className="rounded bg-secondary px-2 py-1 text-xs">
                    {s.name}
                  </span>
                ))}
              </div>
            </div>
          )}
        </div>
      )}

      {/* Peers */}
      <div>
        <h3 className="text-lg font-semibold mb-3 flex items-center gap-2">
          <Globe className="h-5 w-5" /> 发现的 Agent
        </h3>
        <div className="grid gap-3">
          {!peers || peers.length === 0 ? (
            <div className="rounded-xl border bg-card p-8 text-center">
              <Users className="mx-auto h-10 w-10 text-muted-foreground mb-3" />
              <p className="text-muted-foreground">未发现其他 Agent</p>
            </div>
          ) : (
            peers.map((peer) => (
              <div key={peer.agent_id} className="flex items-center gap-3 rounded-xl border bg-card p-4">
                <div className="flex h-10 w-10 items-center justify-center rounded-lg bg-secondary">
                  <Users className="h-5 w-5 text-muted-foreground" />
                </div>
                <div className="flex-1">
                  <p className="font-medium">{peer.name}</p>
                  <p className="text-xs text-muted-foreground">{peer.agent_id}</p>
                </div>
                <span className={`rounded-full px-2 py-0.5 text-xs ${peer.status === 'online' ? 'bg-green-500/10 text-green-600' : 'bg-muted text-muted-foreground'}`}>
                  {peer.status}
                </span>
              </div>
            ))
          )}
        </div>
      </div>
    </div>
  )
}
