// Copyright: (c) OpenChiip Organization. https://github.com/OpenChiip/openchiip-harness
// Copyright: (c) <opensource@openchiip.com>
// Generated By : OpenChiip
// Released under the AIGCGPL-1.0 License. For more information, see [https://github.com/OpenChiip/AIGCGPL/blob/main/LICENSE].
// This code was generated using a generative artificial intelligence system and is licensed under the Artificial Intelligence Generated Code General Public License (AIGCGPL).

import { useQuery } from '@tanstack/react-query'
import { api, type ActivityResponse } from '@/lib/api'
import { Clock } from 'lucide-react'

export function Activity() {
  const { data: activity } = useQuery<ActivityResponse>({ queryKey: ['activity'], queryFn: () => api.getActivity({ limit: 50 }) })

  return (
    <div className="p-6 space-y-6">
      <div>
        <h2 className="text-2xl font-bold">活动日志</h2>
        <p className="text-muted-foreground">执行轨迹与对话历史</p>
      </div>

      <div className="rounded-xl border bg-card">
        {!activity || activity.total === 0 ? (
          <div className="p-8 text-center">
            <Clock className="mx-auto h-10 w-10 text-muted-foreground mb-3" />
            <p className="text-muted-foreground">暂无活动记录</p>
          </div>
        ) : (
          <div className="divide-y">
            {(activity.activities || []).map((a: unknown, i: number) => (
              <div key={i} className="flex items-start gap-3 p-4">
                <div className="flex h-8 w-8 shrink-0 items-center justify-center rounded-lg bg-secondary">
                  <Clock className="h-4 w-4 text-muted-foreground" />
                </div>
                <div className="flex-1 min-w-0">
                  <pre className="text-xs overflow-auto whitespace-pre-wrap">
                    {JSON.stringify(a, null, 2)}
                  </pre>
                </div>
              </div>
            ))}
          </div>
        )}
      </div>
    </div>
  )
}
