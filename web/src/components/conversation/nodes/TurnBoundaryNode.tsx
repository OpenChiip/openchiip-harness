// Copyright: (c) OpenChiip Organization. https://github.com/OpenChiip/openchiip-harness
// Copyright: (c) <opensource@openchiip.com>
// Generated By : OpenChiip
// Released under the AIGCGPL-1.0 License. For more information, see [https://github.com/OpenChiip/AIGCGPL/blob/main/LICENSE].
// This code was generated using a generative artificial intelligence system and is licensed under the Artificial Intelligence Generated Code General Public License (AIGCGPL).

/**
 * TurnBoundaryNode — Turn 开始/结束边界渲染。
 *
 * 借鉴 deepseek-harness 的 turn 边界指示器，
 * 在对话流中展示 turn 的生命周期边界。
 */
import { cn } from '@/lib/utils'
import { ArrowRight, CheckCircle2 } from 'lucide-react'

interface Props {
  kind: 'start' | 'end'
  turn: number
  reason?: string
}

export function TurnBoundaryNode({ kind, turn, reason }: Props) {
  if (kind === 'start') {
    return (
      <div className="flex items-center gap-2 py-1.5">
        <div className="h-px flex-1 bg-border" />
        <div className="flex items-center gap-1.5 text-xs text-muted-foreground">
          <ArrowRight className="h-3 w-3" />
          <span className="font-medium">Turn {turn + 1}</span>
        </div>
        <div className="h-px flex-1 bg-border" />
      </div>
    )
  }

  return (
    <div className="flex items-center gap-2 py-1">
      <div className="h-px flex-1 bg-border/50" />
      <div className="flex items-center gap-1.5 text-xs text-muted-foreground/70">
        <CheckCircle2 className="h-3 w-3" />
        <span>Turn 结束</span>
        {reason && reason !== 'completed' && (
          <span className="text-yellow-600 dark:text-yellow-500">({reason})</span>
        )}
      </div>
      <div className="h-px flex-1 bg-border/50" />
    </div>
  )
}
